{
  "name": "movie-ratings",
  "version": "1.0.0",
  "description": "Movie Ratings - Search, review, and track your favorite movies",
  "private": true,
  "scripts": {
    "start": "parcel index.html",
    "build": "parcel build index.html --public-url ./",
    "clean": "rm -rf dist/ .parcel-cache/",
    "prebuild": "npm run clean"
  },
  "keywords": ["movies", "ratings", "reviews"],
  "author": "group",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^2.8.3"
  }
}